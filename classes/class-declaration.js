// Defining a class in javscript

// * Note: in javascript there is no class actually like c++ or c#
// but we can take some behaviour like class

function MyClass(){
    this.name = "Test Name"
}

// *Note : we always write the class name in camal case. so, that we will know our classes in future



// Using
var myClass = new MyClass(); // class can call using new kayword
console.log(myClass.name); // in console 'test name'

